# Development image: hot reload, sources bind-mounted by docker-compose.dev.yml.
# The production image is in ./Dockerfile.
FROM node:22-alpine
WORKDIR /app
COPY package.json yarn.lock ./
RUN yarn install --frozen-lockfile
COPY . .
EXPOSE 3000
CMD ["yarn", "dev"]
