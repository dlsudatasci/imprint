/**
 * Entry point for the annotation tool.
 *
 * Import from here rather than reaching into the individual files — the state
 * classes and shape helpers inside are implementation detail.
 */
export { default as ReactPictureAnnotation } from "./ReactPictureAnnotation";
export { default as DefaultInputSection } from "./DefaultInputSection";
export { defaultShapeStyle } from "./Shape";
